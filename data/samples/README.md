# Sample Data

This directory will contain small, sanitized network-event fixtures used by automated tests.

Do not commit:

- real credentials
- private keys
- sensitive host information
- unrestricted public network captures
- traffic from systems outside the controlled NetDefender lab

Phase 1 does not require a packet capture. Real lab-derived fixtures will be added after the reconnaissance and parsing phases.
